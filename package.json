{
  "name": "atomic-commit-pipeline",
  "version": "0.1.0",
  "description": "Atomic commit pipeline CLI — propose, apply, review, merge",
  "bin": {
    "atomic": "./bin/atomic"
  },
  "scripts": {
    "test": "echo 'No tests yet' && exit 0"
  },
  "license": "MIT"
}
