cmd_clean() {
  require_git
  rm -f "$ATOMIC_DIR/spec.md" "$ATOMIC_DIR/stage"

  if [[ "$JSON_OUTPUT" == "true" ]]; then
    echo '{"ok":true,"cleaned":true}'
  else
    echo "atomic/ pipeline state cleared."
  fi
}
