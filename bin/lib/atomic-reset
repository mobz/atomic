cmd_reset() {
  require_git
  find "$ATOMIC_DIR" -mindepth 1 -not -name '.gitkeep' -delete

  if [[ "$JSON_OUTPUT" == "true" ]]; then
    echo '{"ok":true,"reset":true}'
  else
    echo "Pipeline reset. atomic/ cleared."
  fi
}
