cmd_show_spec() {
  local content
  content=$(read_atomic_file "$ATOMIC_DIR/spec.md" "$REFS_PREFIX/spec")
  if [[ -n "$content" ]]; then
    echo "$content"
  else
    echo '{"error":"no spec found — run /at:propose first"}' >&2
    exit 1
  fi
}
